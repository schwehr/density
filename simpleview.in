#!/bin/bash

if [ -z "$DYLD_LIBRARY_PATH" ]; then
    export DYLD_LIBRARY_PATH=@FINK@/lib
else
    export DYLD_LIBRARY_PATH=@FINK@/lib:$DYLD_LIBRARY_PATH
fi

# FIX: what if this is in the local directory?
# Then do not want bin.
@FINK@/bin/simpleview_bin $*
